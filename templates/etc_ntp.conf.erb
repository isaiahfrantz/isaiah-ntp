# this config is generated for a template and managed by puppet
# local changes will be automaticaly reverted when puppet runs next
server <%= @ntp_server_a %>  # local clock 
server <%= @ntp_server_b %>  # remote clock